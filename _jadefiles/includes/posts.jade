section.posts__wrap
    h2.section_header
        | Новости и спецпредложения
    ul.posts__list
        | {% for post in site.news %}
        li.post__wrap
            .post__image__wrap
                img.post__image(src='/img/{{ post.image }}', alt='{{ post.title | smartify }}')
            .post__content__wrap
                .post__date
                    | {% assign m = post.date | date: "%-m" %}
                    | {{ post.date | date: "%-d" }}
                    | {% case m %} 
                    | {% when '1' %}января
                    | {% when '2' %}февраля
                    | {% when '3' %}марта
                    | {% when '4' %}апреля
                    | {% when '5' %}мая
                    | {% when '6' %}июня
                    | {% when '7' %}июля
                    | {% when '8' %}августа
                    | {% when '9' %}сентября
                    | {% when '10' %}октября
                    | {% when '11' %}ноября
                    | {% when '12' %}декабя
                    | {% endcase %} 
                    | {{ post.date | date: "%Y" }}

                a.post__link(href='{{ post.url }}')
                    h3.post__title
                        | {{ post.title }}

                p.post__summary
                    | {{ post.summary }}

                .post__categories__list
                    | {% for category in page.categories %}
                    .post__category
                        | {{ post.category }}
                    | {% endfor %}

                footer.post__footer
                    | Читать далее

        | {% endfor %}

