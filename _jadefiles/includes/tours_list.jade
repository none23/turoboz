section.tours_list__wrap
    h2.section_header
        | Подходящее экскурсии
    span#active_tours_counter 
        | {{ site.data.tours | size }}
    ul.tours_list
    | {% for tour in site.data.tours %}
    li.tour_wrap.hidden(class="{% assign tgs = tour.tags | split: ' ' %}{% for tag in tgs %}{{ tag | prepend: 'tagged--' }} {% endfor %}")
        a.tour__link(href='{{ tour.url }}')
            img.tour__image(src='{{ tour.image }}', alt='{{ tour.title }}')
            h4.tour__title
                | {{ tour.title }} 
            h5.tour__subtitle
                | {{ tour.subtitle }} 
            p.tour__intro
                | {{ tour.intro }}
            p.tour__summary
                | {{ tour.summary }}
            time.tour__date
                | {{ tour.date }}
            .tour__length
                | {{ tour.length }}
    | {% endfor %}
        
