include ../mixins.jade
include tour_mixins.jade

mixin EventWrap()
    a.event__wrap(href='/tours/{{ tour.tour }}')
        .event__image__wrap
            img.event__image( sizes="(max-width: 619px) 1px, (min-width: 720px) and (max-width: 1079px) 37vw, (min-width: 1080px) and (max-width: 1158px) 24vw, 263px", srcset="/img/none.jpg 1w, /img/tours200/{{ tour.imgpath }} 200w, /img/tours300/{{ tour.imgpath }} 300w, /img/tours400/{{ tour.imgpath }} 400w", alt="{{ tour.title | smartify }}")
        .event__date
            | {% if tour.dates[0] | plus: 0 > 10 %}
            +LiquidRussianDate('tour.dates[0]')
            | {% else %} по заказу {% endif %}

        h4.event__title
            | {{ tour.title }}.
            | {{ tour.subtitle }}
        footer.event__footer
            | Подробнее

section.upcoming_events__wrap
    h2.section_header
        | Ближайшие туры
    .upcoming_events__list
        | {% assign upcoming_counter = 0 %}
        | {% for x in site.data.upcoming%}
        |     {% assign tour = site.data.tours[x] %}
        |     {% if tour.dates[0] != 0 %}
        |         {% if upcoming_counter > 3 %}
        |             {% break %}
        |         {% else %}
        |             {% assign upcoming_counter = upcoming_counter | plus: 1 %}
        +TourCard('tour')
        |         {% endif %}
        |     {% endif %}
        | {% endfor %}
